name: centrifugo_flutter_exams
description: Centrifugo is Scalable real-time messaging server used for multiplayer game and messenger and other real time topics

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.3.0
  protobuf: ^2.0.0
  fixnum: ^1.0.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:

  uses-material-design: true
